# Featured 16 native UI proof

Actual React frontend and disposable Convex backend with local staff authentication. No response mocks or production writes. All publication and editorial editing occurred only in the disposable fixture backend; no digest was sent.

- Staff: eight editorial reservations plus eight telemetry slots. Five editorial artifacts ready and three unavailable: 13 ready cards, three pending cards without links. Skills show 16 telemetry cards.
- Real editor reorder/reason/save updates the audited data owner. The old report becomes visibly stale; refresh uses the saved revision. Published snapshots remain byte-identical during editing, verified through the staff API.
- Public homepage: first load defaults to Featured; both catalogs show 16 approved identities in exact order. Pending identities never become cards. Official, New and Trending remain reachable directly.
- Empty native install aggregates yield 0/16 skill recommendations with an explicit 16-place shortfall, despite 16 incumbent badges. The original aggregate fixture was restored afterward.
- Four viewports (1440, 1024, 768, 390): no horizontal overflow or uncaught page errors across populated, pending, editor, empty and public surfaces.
- Mobile defect caught during inspection: a long publisher handle squeezed the title to 0px. Three CSS declarations allow the handle to shrink and show an ellipsis, restoring the title to 72.7px on identical input.
- The public owner regression fails on original badge timestamp order, then passes for plugin page, skill public V4, legacy highlighted list, listWithLatest and paginated skill package catalog. Limit-one pagination traverses the entire selection.
- 469 focused and 23 legacy tests pass; full TypeScript and cold native Convex deployment pass. Parent owns full combined repository gates.

The original 50726 staff baseline uses the previous 10-plugin/eight-selection fixture. The new 20+20 fixture exercises the expanded 16-slot contract. These screenshots compare behavior; they do not claim a ranking change on identical input. The mobile pair and public-order RED/GREEN use identical data. Local public API requests carry a fixture token because the native backend rejects anonymous requests outside the edge origin; responses were never replaced.

Only allowlisted screenshots, this summary/report and the interaction video are publishable. Local auth snapshots, fixture token/config, backend databases and process logs are excluded.

## Saved-report publication binding

Fresh native backend at local commit `45a92c6061`, with parent `8c4f9146e6` publication owner bytes unchanged. The existing fixture owner generated both complete 16-entry proposals through the stock asynchronous report API. Requests were derived from actual completed reports, including all editorial and telemetry counts.

The built staff CLI default dry run preserved all protected state. Explicit local apply published exactly 16 identities in each catalog and saved the authoritative report ID and result hash. Retained badges and digest rows remained unchanged.

Twenty native rejection checks (ten per catalog) all returned explicit 400/409 errors with identical before/after protected-state hashes: missing report ID; changed 30-day count; changed final-seven-day count; changed order; eligible unselected identity; other-catalog report; demand report; expired report; a current version changed after report generation; stale publication timestamp.

Expiration and latest-version changes were explicit local fixture transitions through the stock import CLI. The current-version check appended a clean fixture version and repointed the local latest-version fields, then restored the original pointer. This proves the actual publication transaction's version check; it is not a separate full registry publish lifecycle test. No production data or digest delivery was involved. Sanitized outcomes and provenance hashes are embedded in `summary.json`.
