# Integrated-head source review

**No findings.** Reviewed PR head `8fac071cc08628959a73b0961e052ce29659a47e`; validated integration head `a1cb5b376d5c564dbf60345b2eddaaa5efd1ae60` combines it with base main `a5b31b2f3353fda19ee808ef71ffa3f5cbc7bc44`. This is a bounded integration review of [#3765](https://github.com/openclaw/clawhub/pull/3765), [#3766](https://github.com/openclaw/clawhub/pull/3766), and [#3720](https://github.com/openclaw/clawhub/pull/3720), not a fresh exhaustive review of those features.

The six overlapping files are `convex/skills.ts`, `convex/packages.ts`, `convex/httpApiV1/shared.ts`, `convex/_generated/api.d.ts`, `convex/httpApiV1.shared.test.ts`, and `convex/skills.versions.public.test.ts`. For every overlap, the visibility branch's complete added/removed line sequence is identical before and after the merge, compared against the corresponding main baseline. The other 19 visibility-branch files are byte-identical to the prior head. Git's remerge diff is empty. Exact source blobs and comparison hashes are in `integrated-head-review.json`.

Manual semantic checks:

- Featured order is applied to existing candidates; it neither turns saved snapshot entries into new catalog membership nor copies their stored version strings into visibility responses. Skills retain active-owner/public conversion and published latest-version loading; the new highlighted catalog path uses existing digest visibility/filter/projector functions. Packages retain `canViewerReadPackage` and existing discovery filters before ordering. Existing digest synchronization assumptions remain; this does not claim arbitrary historical stale digests were repaired.
- The checked public selectors, parent/version binding, pending/blocked/deleted exclusions, legacy publication handling, owner previews, malware metadata exception, raw internal reads, 25/200 hash bounds, and separate byte-download policy are preserved. Tag hydration still requires the returned current parent's tag mapping. Feature ordering does not replace those readers.
- The one shared error-helper addition strips stack frames after a newline; it does not alter authorization/status selection, batch limits, or current-parent checks. Its paired formatter test coexists with current-tag tests. The version-query test adds only the new Featured table's empty fixture response.
- Newly exported Featured actor helpers remain ordinary TypeScript helpers. Registered new editor/publish routes and queries require authenticated moderator access; internal wrappers also validate the actor. No public raw version reader was introduced by this merge.

Schema/build impact: the integrated schema exactly matches new main. It adds `featuredSelections` with its index and retention registration, and widens report versions from v1 to v1/v2. Existing publication schemas are unchanged. The new table is empty-compatible and prior report rows remain valid, so this delta does not require a destructive migration or historical backfill. The ordinary backend deployment must include the new table/index and code. New schema source/dist exports are inherited intact from main, and `node_modules/clawhub-schema` resolves to this integration worktree's `packages/schema`. Generated API declarations retain all three new upstream modules plus the visibility helper module.

Prior evidence remains inherited for the unchanged source: the structured-review corrections preserve malware hash inspection and current tag mapping; `8fac071cc0` is only the reviewed old-CLI 404 expectation correction, whose real native scenario passed before this merge. Those results are not relabeled as exact merged-head passes. Parent owns integrated static/unit/types/build gates, and the HTTP lane owns the fresh 187-case native run. This agent launched no duplicate tests/autoreview and made no source edits or public writes.

The integration commit is a local validation candidate, not a new PR head. Parent will retain the reviewed PR head, verify the base remains current, await merged-tree gates, and require the landed squash commit tree to equal `4d851982996718bcfc52d43c09da808076986870`. Prior PR head and merged tree are not claimed to be runtime-equivalent wholesale: the added Featured changes are expected. No landing or deployment is attested by this source review.
